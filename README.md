# swift-memory-aligned-primitives

Fixed-size, alignment-guaranteed owned byte region — `Memory.Aligned`.
Re-domained from `Buffer.Aligned` in the MSB tower W3 re-layering (owned
region, no occupancy ⇒ Memory tier; `Buffer.Protocol` conformance dropped).
